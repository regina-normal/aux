# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = "regina/win10"
  # config.vm.synced_folder ENV['WORKSPACE'] + '/regina', "/regina", id: 'regina'
  config.vm.synced_folder ENV['WORKSPACE'] + '/aux', "/aux", id: 'aux'
end
