These are jenkins scripts used for building release packages on the
"primary architectures" (i.e., x86_64, where we also include source packages,
and where relevant also i386, where we want only the binary packages).

It is assumed that the build machine uses x86_64 as its default architecture.
