These are jenkins scripts used for building release packages on
"port architectures" (i.e., architectures where we want just binary
packages, not source packages).

It is assumed that this port architecture is the default architecture
for the build machine.
