These are jenkins scripts used for nightly builds direct from the git
repository.  These build x86_64 and (where relevant) i386 packages.

It is assumed that the build machine uses x86_64 as its default architecture.
