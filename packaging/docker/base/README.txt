
To build the base/* images, run the corresponding scripts as root from this
directory, passing the distribution version as an argument.  For i386 builds
(currently only supported for debian/ubuntu), pass i386 as a second argument.

  ./debian bullseye
  ./debian bullseye i386
  ./ubuntu jammy
  ./fedora 36
  ./opensuse 15.4
  ...

